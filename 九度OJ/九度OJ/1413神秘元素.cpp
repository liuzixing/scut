#include <iostream>
#include <string.h>
#include <cstdio>
using namespace std;

int n;
bool ans[100001];
int a[100001],f[100001],l,r,m,len;
int main(){
	while (~scanf("%d",&n)){
		memset(ans,0,sizeof ans);
		memset(f,0,sizeof f);
		memset(a,0,sizeof a);
		for (int i = 0;i < n;i++)
			scanf("%d",&a[i]);
		ans[1] = a[0];
		len = 1;
		for (int i = 1;i < n;i++){

		}
	}
}